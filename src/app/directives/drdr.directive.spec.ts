import { DrdrDirective } from './drdr.directive';

describe('DrdrDirective', () => {
  it('should create an instance', () => {
    const directive = new DrdrDirective();
    expect(directive).toBeTruthy();
  });
});
